{
  "name": "barbacue",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "scrape": "npm run scrape --workspace=packages/scraper",
    "seed": "npm run seed --workspace=packages/scraper",
    "desktop": "npm start --workspace=@barbacue/desktop",
    "desktop:windows": "npm run dist:win --workspace=@barbacue/desktop",
    "test:desktop": "npm test --workspace=@barbacue/desktop && npm test --workspace=@barbacue/print-agent"
  },
  "devDependencies": {
    "playwright-extra": "^4.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "typescript": "^5.5.0"
  }
}
